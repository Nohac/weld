[project]
name = "weld-remote"
version = "0.1.0"
description = "Restricted Bevy Remote Protocol client for Weld development"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
weld-debug = "weld_remote.cli:main"

[build-system]
requires = ["uv_build>=0.11.0,<0.12.0"]
build-backend = "uv_build"
